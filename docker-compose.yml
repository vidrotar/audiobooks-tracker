version: '3.8'

services:
  audiobook-library:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
      - ./backend/server.js:/app/server.js
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/audiobooks.db
    restart: unless-stopped